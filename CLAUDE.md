# Claude Agent Configuration

## Settings

- Disable Claude attribution in commits and pull requests
- Do not add "Generated with Claude Code" comments to commits
- Do not add "Co-Authored-By: Claude" to commits

## Package Manager

- Always use `pnpm` instead of `npm` for all package management operations
- Use `pnpm run` for executing scripts
- Use `pnpm install` for installing dependencies
